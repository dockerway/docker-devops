query getDockerNodes($environment:ID!){
    getDockerNodes(environment:$environment){
        id
        hostname
        ip
        role
        leader
        reachability
        availability
        engine
        state
    }
}
