query findEnvironmentService($id:ID!){
    findEnvironmentService(id:$id){
        id
        environment{
                id
                name
            }
        service{
                id
                name
            }
        stack
    }
}
