mutation createService($input: ServiceInput){
    createService(input: $input){
        id
        name
        description
        platform{
                id
                name
            }
        volumes
        ports
        variables{
            name
            defaultValue
        }
    }
}

