query findService($id:ID!){
    findService(id:$id){
        id
        name
        description
        platform{
            id
            name
        }
        volumes
        ports
        envs{
            name
            defaultValue
        }
    }
}
