query findService($id:ID!){
    findService(id:$id){
        id
        name
        description
        image
        repository
        platform{
            id
            name
        }
        volumes
        ports
        envs{
            name
            defaultValue
        }
    }
}
