mutation updateEnvironment($id: ID!, $input: EnvironmentInput){
    updateEnvironment(id: $id, input: $input){
        id
        name
        dockerApiUrl
        dockerApiToken
        type
    }
}

