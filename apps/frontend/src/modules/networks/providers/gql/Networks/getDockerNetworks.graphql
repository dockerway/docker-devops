query getDockerNetworks($environment:ID!){
    getDockerNetworks(environment:$environment){
       Name
        Id
        Created
        Scope
        Driver
        EnableIPv6
        IPAM{
            Driver
            Options
            Config{
                Subnet
                Gateway
            }
        }
        Attachable
    }
}
